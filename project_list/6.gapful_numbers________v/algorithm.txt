1.숫자를 문자형으로 입력받는다.
2.arr과 arr[strlen(arr)]의 주소만 따로 떼와서 붙여 문자형으로 만든다.
3.입력받은 문자형과 따로 만든 문자형 모두 atoi 함수에 넣는다.
//stdio.h에 들어있다
//atoi는 아스키 to 정수(integer), aiof는 아스키 to 실수(float)
4.정수형으로 모듈 연산을 한다.
5.0이면 참, 아니면 거짓
